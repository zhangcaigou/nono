# Install the repository's custom Transformers fork first:
#   python -m pip install -e ./transformers
# PyTorch is intentionally omitted because its CUDA wheel must match the host.
accelerate>=0.26
arxiv>=2.1
beautifulsoup4>=4.12
lxml>=5.0
requests>=2.31
rich>=13.0
safetensors>=0.4
tqdm>=4.66
wandb>=0.17
